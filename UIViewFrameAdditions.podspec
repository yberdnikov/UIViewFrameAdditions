Pod::Spec.new do |s|
  s.name     = 'UIViewFrameAdditions'
  s.version  = '1.0.0'
  s.license  = 'MIT'
  s.summary  = "UIView frame property additions - for easy set/get frame values."
  s.authors  = { 'Yuriy Berdnikov' =>
                 'yberdnikov@me.com' }
  s.social_media_url = "https://twitter.com/yberdnikov"
  s.source   = { :git => 'https://github.com/yberdnikov/UIViewFrameAdditions.git', :tag => '1.0.0' }
  s.source_files = 'UIViewFrameAdditions'
end
